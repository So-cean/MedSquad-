# MedSquad 项目描述与策划需求

> 本文档供策划方阅读。目标是让你在 10 分钟内理解这个项目"是什么、做到了哪、需要你设计什么"。

---

## 一、项目概述

**MedSquad**（内部代号 EDMAS — Emergency Department Multi-Agent System）是一个以**医院急诊科**为背景的 **2D 俯视角模拟游戏**。所有 NPC 由大语言模型（LLM）驱动，能自主对话、分诊、移动、记忆——玩家是观察者，也可以介入互动。

**核心卖点**：不是脚本对话，不是选项树——NPC 的每一句话都是 LLM 实时生成的，基于各自的记忆、角色、当前病情、对话历史。每个患者就诊流程都不一样。

---

## 二、玩法风格参考：斯坦福小镇（Smallville）

参考论文：*Generative Agents: Interactive Simulacra of Human Behavior*（Park et al., Stanford 2023）。

### 借鉴的核心设计
| Smallville 机制 | MedSquad 对应 |
|---|---|
| 25 个 AI agent 在沙盒小镇生活 | 多个 AI 医护 + 患者 在急诊科流动 |
| Agent 有性格、关系、日程 | 医护有角色定位（分诊护士/急诊医生/检验员），患者有隐藏病例 |
| **Memory Stream** — 记住所有交互 | `MemoryStore` 每个 NPC 独立 JSON 记忆，按对话伙伴检索 |
| **Reflection** — 反思过去 | （待实现）医护复盘接诊过程 |
| **Planning** — 做计划再行动 | `Scheduler` 调度患者流：分诊→医生→出院 |
| Agent 自主起床/上班/社交 | NPC 自主走位、主动问诊、自动结束对话 |
| 玩家可观察可介入 | 玩家操控医生角色在院内走动，可触发 NPC 对话 |

### 与 Smallville 的关键差异
- **场景更聚焦**：不是开放小镇，是封闭急诊科（3 层楼）。事件密度更高，节奏更快。
- **强流程驱动**：急诊有标准流程（分诊→诊室→处置→出院），不是完全自由沙盒。LLM 在流程框架内自主决策。
- **医疗专业性**：NPC 需要表现出现实医护的问诊逻辑（开放式提问→聚焦→决策），不是日常闲聊。
- **单次会话**：当前是一次性场景（5 个患者走完流程即结束），不是 24 小时持续模拟。可扩展为持续运行。

---

## 三、项目背景：医疗场景 + Multi-Agent

### 为什么选医疗
急诊科是**高密度多角色协作**的典型场景：
- **多角色**：分诊护士、急诊医生、抢救医生、检验员、质控员、住院护士、患者
- **强流程**：分诊→诊室→检查→处置→转归/出院，每步有明确角色切换
- **信息不对称**：患者知道自己症状但不懂医；医护懂医但看不到患者隐藏病例——必须通过对话采集信息
- **资源竞争**：有限的护士/医生/设备，多个患者同时就诊需要排队调度
- **决策多样性**：同一症状可能走不同处置路径（轻症出院 / 重症转抢救 / 需要检查）

### Multi-Agent 架构
不是"一个 LLM 扮演所有角色"——是**每个 NPC 独立调用 LLM**，各自有：
- 独立的身份（名字、角色、病例/职责）
- 独立的记忆（只记得自己经历过的对话）
- 独立的 LLM 请求（带自己的上下文：角色 prompt + 记忆 + 当前对话历史）
- 独立的状态机（ARRIVED → WAITING → GOING_TO_ROOM → BEING_EXAMINED → DISCHARGED）

NPC 之间的对话是**回合制 LLM 交替**：护士说一句 → 患者回一句 → 护士说一句……每句都是独立 LLM 调用，每次都带完整对话历史 + 自己的记忆。

---

## 四、当前技术实现

### 技术栈
| 项 | 选择 |
|---|---|
| 引擎 | Godot 4.7（2D，Forward+ 渲染） |
| 语言 | GDScript |
| LLM | DeepSeek-V4-Flash（通过 Gitee AI API，OpenAI 兼容接口） |
| 字体 | NotoSansSC（中文） |
| 无后端 | 所有 LLM 调用从游戏客户端直发，无 Python/Node 中间层 |
| 无插件 | 纯原生 Godot 实现，未用第三方 addon |

### 四层架构

```
┌─────────────────────────────────────────────┐
│  Dispatch 调度层                              │
│  NpcManager / Scheduler / ConversationContext │
│  职责：NPC 生命周期、患者流调度、对话配对       │
├─────────────────────────────────────────────┤
│  LLM 层                                       │
│  NpcFsm（stateless）→ HTTPRequest → Gitee AI  │
│  职责：发 prompt、收 JSON、解析+兜底           │
├─────────────────────────────────────────────┤
│  Memory 记忆层                                │
│  MemoryStore（per-NPC JSON 磁盘持久化）        │
│  职责：存对话节点、按伙伴检索上下文             │
├─────────────────────────────────────────────┤
│  Display 显示层                                │
│  DialogueManager / DialogueBubble / NpcHoverCard │
│  职责：气泡池、打字机、hover 卡片              │
├─────────────────────────────────────────────┤
│  Map 地图层                                   │
│  MapSystem / HospitalMapData / BaseNpc        │
│  职责：NavigationRegion2D、寻路、Area 触发     │
└─────────────────────────────────────────────┘
```

### Autoload 单例（7 个）
`DialogueManager` → `TimeSystem` → `NpcManager` → `HospitalMapData` → `MapSystem` → `ResourceRegistry` → `Scheduler`

### 对话回合循环
```
Scheduler 派发 InteractionSession
  ↓
患者 approach_and_face(医护) → 面对面
  ↓
医护先发起 LLM（开放式问诊）
  ↓
LLM 返回 JSON: {think, responses:[{target, utterances, conversation_done}], next_step}
  ↓
显示气泡（think 蓝色打字机 → 清空 → utterance 黑色打字机 → fade）
  ↓
写记忆（双方互写：speaker→listener, listener→speaker）
  ↓
等阅读时间 → 切到对方 → 对方调 LLM → 循环
  ↓
conversation_done=true → Session 结束 → Scheduler 派发下一阶段（医生/出院）
```

### 安全机制
- **最大对话轮数**：8 轮（4 医护 + 4 患者）强制结束，防止 LLM 无限循环
- **Wall-clock 超时**：30 秒无进展强制 discharge
- **空 utterance 过滤**：LLM 返回空串自动替换为 "..."，不显示空白气泡
- **LLM 节流**：两次请求间最少 0.5 秒，避免 API 压力
- **双 API key 轮换**：单个 key 限流时自动切换

---

## 五、NPC 角色体系

### 现有 NPC（11 种视觉类型）

| NPC | 显示名 | 角色 | 所在楼层 | 帧 |
|---|---|---|---|---|
| Nurse white | 分诊护士 | triage_nurse | F1 ED Core | 8方向 idle+walk |
| Nurse blue | 急救护士 | nurse | F1 ED Core | 8方向 |
| Nurse green | 住院护士 | nurse | F3 Downstream | 8方向 |
| Scrubs green | 手术医生 | doctor | F2 Diagnostics | 8方向 |
| Scrubs blue | 检验科护士 | nurse | F2 Diagnostics | 8方向 |
| Patient green | 患者 | patient | F1（spawn@入口） | 8方向 |
| Patient blue | 患者 | patient | F1（spawn@入口） | 8方向 |
| **Doctor red** | 急诊医生红 | doctor | F1 ED Core | 8方向（新增） |
| **Doctor green** | 急诊医生绿 | doctor | F1 ED Core | 8方向（新增） |
| **Labrad blue** | 检验员蓝 | technician | F2 Diagnostics | 8方向（新增） |
| **QA gray** | 质控员灰 | qa | F3 Downstream | 8方向（新增） |

### 角色 prompt 现状
- ✅ `triage_nurse` — 不能读隐藏病例，开放式问诊，3-5 轮决定 next_step
- ✅ `doctor` — 不能读隐藏病例，根据口述+分诊记录决策，3-5 轮
- ✅ `patient` — 大白话描述症状，不背医学术语，只回答被问的
- ❌ `technician` — 无 prompt（labrad blue 静态存在，不对话）
- ❌ `qa` — 无 prompt（qa gray 静态存在，不对话）

### NPC 行为
- **默认静止**（`can_wander=false`）：NPC 不自由移动，站原地等指令
- **被指令时移动**：`walk_to(location)` 用 NavigationAgent2D 绕墙寻路
- **软碰撞**：NPC 之间 35px 半径软推开，不依赖物理体
- **面对面**：`approach_and_face(target, offset)` 走近后自动转向对方

---

## 六、世界设定

### 3 层楼（3 张地图）

| 楼层 | 名称 | 主题 | 命名位置 |
|---|---|---|---|
| **F1** | ED Core 急诊核心接入区 | 分诊、诊室、抢救、入口 | ED_ENTRANCE, TRIAGE, WAITING_AREA, DOCTOR, ED_RESUS, ELEVATOR |
| **F2** | Diagnostics 检查诊断区 | 检验、影像、候检、报告 | LAB, IMAGING, DIAGNOSTIC_WAITING, RESULT_REVIEW, ELEVATOR |
| **F3** | Downstream 转归下游区 | ICU、病房、处置、出院、留观 | ICU, WARD, DISPOSITION, ED_BOARDING, DISCHARGE, DISCHARGE_ADMIN, ELEVATOR |

### 当前运行场景
- **主 playtest 场景**：`scens/edmas/DialogueTest.tscn`（只加载 F1 map1）
- **Floor 场景**：`Floor_ED_Core.tscn` / `Floor_Diagnostics.tscn` / `Floor_Downstream.tscn`（用于切楼层时加载）
- **楼层切换**：`MapSystem.change_floor(id)` 已实现，但未接入玩家电梯交互

### 患者流（标准流程）
```
5个患者 spawn @ ED_ENTRANCE
  ↓ Scheduler 派发分诊
排队 → 空闲护士接诊 → 面对面 LLM 对话（3-5轮）
  ↓ 护士决定 next_step
轻症 → discharge 出院
重症 → walk_to DOCTOR 诊室 → 医生接诊 LLM 对话 → discharge
危重 → walk_to ED_RESUS 抢救 → 医生接诊 → discharge
  ↓
患者 walk_to ED_ENTRANCE → 消失
  ↓
全部出院 → 场景结束
```

---

## 七、对话系统规格

### 气泡 UI
- **位置**：NPC 头顶上方 20px，跟随 NPC 移动
- **两阶段**：THINK（🤔 蓝色，内心独白）→ 清空 → UTTERANCE（🗣 黑色，说出口的话）
- **打字机**：0.06 秒/字
- **高度**：1-3 行自适应；超 3 行固定 3 行高度，自动滚到底显示最后 3 行
- **无滚动条**：scrollbar 隐藏
- **中文断行**：`language="zh"` + `AUTOWRAP_WORD`（ICU CJK 断词）

### LLM 返回格式
**医护返回**：
```json
{
  "think": "推理/感受（可选）",
  "responses": [{"target": "patient_001", "utterances": ["对患者说1句话"], "conversation_done": false}],
  "next_step": null | {"next_role": "doctor|discharge", "target_room": "DOCTOR|ED_RESUS", "orders": []}
}
```

**患者返回**：
```json
{
  "think": "内心感受（可选）",
  "utterances": ["10-20字大白话"]
}
```

### 记忆系统
- 每个 NPC 独立 JSON 文件：`user://npc_memories/{node_name}/nodes.json`
- 存对话节点：speaker_id, listener_id, think, text, importance, keywords
- 检索：`get_context_for_partner(partner_id, count)` — 只取和特定伙伴的对话历史
- 跨 NPC 路由：患者说话 → 同时写入护士记忆（标注 patient_id）；护士回应 → 同时写入患者记忆

---

## 八、玩家角色

### 当前实现
- **角色**：医生 white（玩家操控）
- **移动**：WASD 键盘 / 虚拟摇杆（移动端）
- **相机**：Camera2D zoom 0.6，鼠标滚轮缩放，中键拖拽，触屏双指
- **交互**：走近 NPC 触发 `InteractionZone`（50px 半径 Area2D），显示"按 E"
- **限制**：玩家目前只是观察者，不能参与 LLM 对话流程（对话是 NPC 之间自主进行）

### 玩家在流程中的位置
- **不参与分诊/诊室对话**：那是 NPC 之间的事
- **可观察**：走到任何对话现场，看气泡、听内容
- **可查询**：hover NPC 看状态卡片（名字/角色/状态/位置/行为）
- **待设计**：玩家能做什么有意义的事？这是策划需要回答的核心问题之一

---

## 九、已完成 vs 待策划

### ✅ 已完成（可运行）
- 4 层架构 + 7 个 autoload 单例
- LLM 集成（DeepSeek-V4-Flash，双 key 轮换，节流，JSON 解析+兜底）
- 11 种 NPC 视觉素材（8 方向 idle+walk，全部切片就绪）
- 3 层楼地图 + 碰撞 + 寻路（NavigationRegion2D 烘焙）
- 患者流调度（Scheduler + InteractionSession + ResourceRegistry 排队）
- 对话气泡（think/utterance 双阶段，打字机，3 行限制，中文断行）
- 记忆系统（per-NPC JSON，按伙伴检索）
- hover 卡片（NPC 状态查询）
- 4 个 bug 修复（气泡超 3 行、护士负载均衡、患者卡死、空 utterance）
- 代码重构（删死代码、合并子类脚本、FontRegistry、_ensure_nav_agent）

### ❌ 待策划设计（需要你回答）

#### 1. 玩家角色定位
- 玩家在游戏里是什么身份？（实习医生？护士长？院长？患者家属？）
- 玩家能影响 NPC 决策吗？（建议？质疑？接管？）
- 玩家有目标吗？（治愈多少患者？发现某病例？管理效率？）
- 失败条件是什么？

#### 2. 流程扩展
- 当前只有 triage→doctor→discharge。要加检查环节吗（CT/lab/ECG）？
- 检查环节的 NPC 互动？（labrad blue 检验员如何参与）
- 转归环节？（ICU/病房/留观，qa gray 质控员如何参与）
- 多次往返？（患者检查完回诊室，医生看结果再决定）

#### 3. NPC 自主性深度
- 当前 NPC 只在被 Scheduler 指令时移动。要加自由行为吗？（护士巡台、医生查房、患者上厕所）
- Smallville 的 reflection 机制要吗？（医护下班前复盘今天接诊）
- 日程表？（早班/夜班、交接班）
- NPC 之间的关系？（医护同事关系影响协作？患者之间交流？）

#### 4. 时间系统
- `TimeSystem` 已有（1 秒真实 = 1 分钟游戏），但没接入任何系统
- 要不要加时间压力？（患者等太久恶化？换班强制结束就诊？）
- 一局多长？（5 分钟？30 分钟？持续运行？）

#### 5. 资源管理深度
- 当前 ResourceRegistry 只有护士/医生排队。要加设备吗？（CT 机、化验仪占用）
- 房间容量？（抢救室同时只能 2 个患者？）
- 物资？（药品/耗材库存？）—— 可能太重，看你要不要

#### 6. 多结局 / 评分
- 一局结束后给评分吗？（治愈率、平均就诊时间、误诊率）
- 不同患者病例预设？还是 LLM 即兴生成？
- 教学价值？（这是不是可以做成医学教育工具？）

#### 7. technician / qa 角色 prompt
- labrad blue（检验员）该说什么 prompt？给患者抽血时说什么？出报告时说什么？
- qa gray（质控员）该做什么？抽审病例？找医护谈话？
- 这两个角色要不要参与 LLM 对话，还是纯环境 NPC？

#### 8. 美术 / 叙事基调
- 写实医疗？还是轻松医疗？（影响对话语气、病例严肃度）
- 有没有"故事线"？（某天来个特殊患者串起整局）
- 中文医疗术语规范？（用"主诉"还是"哪里不舒服"）

---

## 十、技术约束（策划需知）

- **无后端**：LLM 调用从客户端直发，不能做需要服务端逻辑的功能（如多人、持久排行榜、反作弊）
- **LLM 延迟**：每次调用 1-6 秒，回合制对话可接受，但实时动作不行
- **LLM 成本**：DeepSeek-V4-Flash 便宜，但一局 5 患者 × 8 轮 × 2 方 ≈ 80 次调用，需控制
- **无 npm/构建系统**：纯 Godot 编辑器工作流
- **单机**：当前是单机运行，不支持联机
- **中文优先**：所有 UI、prompt、病例都是中文，策划输出请用中文

---

## 十一、参考文档

- `AGENTS.md` — 项目结构、coding rules、auto-record 规则
- `REQUIREMENTS.md` — 需求规格（对话显示、NPC 行为、LLM 调用、游戏流程）
- `issues.md` — 已知问题与修复记录
- `scripts/edmas/scheduler/prompt_context.gd` — 现有 LLM prompt 全文
- `scripts/edmas/scenario_config.gd` — 默认场景配置（2护士+2医生+5患者）

---

## 十二、对策划的请求

请基于以上信息，输出一份**策划方案**，重点回答：

1. **核心玩法循环**：玩家在一局里具体做什么、获得什么反馈、为什么想再来一局
2. **NPC 自主性与玩家介入的平衡**：参考 Smallville，但医疗场景下玩家该多有存在感
3. **流程设计**：完整的患者就诊路径（含检查/转归），各 NPC 角色在路径上的职责
4. **技术员/质控员角色**：labrad blue 和 qa gray 怎么参与（对话？行为？纯环境？）
5. **一局的结构**：时长、患者数量、难度曲线、结束条件、评分
6. **时间与节奏**：游戏时间怎么走、有没有时间压力、换班机制
7. **叙事与病例**：病例怎么设计（预设/生成）、有没有故事线、医疗专业度把控
8. **MVP 范围**：如果只能先做最小可玩版本，砍什么留什么

输出格式不限，但请给出**可执行的设计决策**（不是开放性讨论），我们的工程实现会严格按你的方案落地。
